<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.ActionBar">
        <item name="android:windowActionBar">true</item>
        <item name="android:windowNoTitle">false</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="SaveButtonDisabled">
        <item name="android:backgroundTint">@color/colorCannotSave</item>
        <item name="android:src">@android:drawable/ic_menu_close_clear_cancel</item>
    </style>

    <style name="SaveButtonEnabled">
        <item name="android:backgroundTint">@color/colorCanSave</item>
        <item name="android:src">@android:drawable/ic_menu_save</item>
    </style>

    <style name="EinfachSparen.Button" parent="android:style/Widget.Material.Button"></style>


</resources>
